import QrCodeToFrontendMentorCard from "@/components/qr-code-to-frontend-mentor-card";

export default function TryFrontendMentor() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center">
      <QrCodeToFrontendMentorCard />
    </main>
  );
}
